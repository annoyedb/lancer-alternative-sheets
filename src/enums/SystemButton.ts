// Buttons that are not macros but baked as flows in the system
// These are used for the button factory so that removing them from 
// the sheets does not permanently lose them
export enum SystemButton
{
    Stabilize = "Stabilize",
    Overcharge = "Overcharge",
    BasicAttack = "BasicAttack",
    Damage = "Damage",
    TechAttack = "TechAttack",
}