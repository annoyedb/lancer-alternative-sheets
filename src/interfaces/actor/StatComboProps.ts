export interface StatComboProps
{
    icon: string;
    label: string;
    value: number;
    valuePath?: string;
    outerStyle?: Array<string>;
    innerStyle?: Array<string>;
    editable?: boolean;
}